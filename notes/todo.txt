[ ] when outputting code we should check if a function is using a variable whose namespace is that function, and emit a 'static' definition for it.
[ ] create data sections for DLLs
    - it looks like all DLL data sections are one after the other, and each one ends with its function table.
